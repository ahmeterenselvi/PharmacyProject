@{
    Layout = null;
}

<!DOCTYPE html>
<html>

@await Html.PartialAsync("~/Views/Default/HeadPartial.cshtml")

<body>

    <div class="hero_area">
        <header class="header_section">
            @await Html.PartialAsync("~/Views/Default/TopHeaderPartial.cshtml")
            @await Html.PartialAsync("~/Views/Default/NavbarPartial.cshtml")
        </header>
    </div>

    @RenderBody()

    <section class="info_section ">
        <div class="container">
            @await Html.PartialAsync("~/Views/Default/FooterPartial.cshtml")
        </div>
    </section>

    @await Html.PartialAsync("~/Views/Default/ScriptsPartial.cshtml")

</body>

</html>